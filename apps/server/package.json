{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm start",
    "build": "npm run build",
    "test": "npm test",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts*\"",
    "clean": "npm run clean",
    "server": "npm run server"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "esbuild-register": "^3.3.2",
    "eslint": "^7.32.0",
    "eslint-config-custom-server": "*",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
